---
deployment:
  tasks:
    - /bin/mkdir -p /home/linhercom/public_html/move
    - /opt/cpanel/ea-nodejs20/bin/npm ci --prefix frontend || /opt/cpanel/ea-nodejs18/bin/npm ci --prefix frontend || /usr/local/bin/npm ci --prefix frontend || /usr/bin/npm ci --prefix frontend
    - /opt/cpanel/ea-nodejs20/bin/npm run build --prefix frontend || /opt/cpanel/ea-nodejs18/bin/npm run build --prefix frontend || /usr/local/bin/npm run build --prefix frontend || /usr/bin/npm run build --prefix frontend
    - /bin/test -f frontend/dist/index.html
    - /bin/cp -R frontend/dist/. /home/linhercom/public_html/move
